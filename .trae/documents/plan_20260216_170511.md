Vou compilar todas as informações coletadas sobre a arquitetura atual (Vite, React, Supabase, Power BI Embedded, Azure Function) em um prompt detalhado em Markdown. 

Este prompt servirá como um "guia mestre" para que você (ou outra IA) possa recriar o sistema do zero em um novo projeto, contendo:

1.  **Visão Geral e Tech Stack**: Definição das tecnologias (React, Vite, TS, Tailwind, Supabase).
2.  **Estrutura de Banco de Dados**: O SQL exato para criar as tabelas `tv_presentations` e `tv_presentation_slides` no Supabase, incluindo as políticas de segurança (RLS).
3.  **Autenticação e Backend**: O código da Serverless Function (`api/powerbi.ts`) necessária para autenticar com a Microsoft de forma segura (Service Principal), evitando expor chaves no frontend.
4.  **Serviços de Frontend**: O código do `powerBiApiService.ts` para consumir a API e gerenciar tokens.
5.  **Lógica de Apresentação**: Descrição detalhada e trechos de código para o `TVPresentationViewer` (player de slides) e `TVPresentations` (gerenciador).
6.  **Configuração de Ambiente**: Lista das variáveis necessárias no `.env` (Supabase, Azure/Microsoft).

Ao confirmar, irei gerar este conteúdo Markdown para você.